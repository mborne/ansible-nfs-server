galaxy_info:
  author: mborne
  description: Install NFS server for DEV purpose

  license: MIT

  min_ansible_version: "2.9"

  # To view available platforms and versions (or releases), visit:
  # https://galaxy.ansible.com/api/v1/platforms/
  platforms:
  - name: Ubuntu
    versions:
    - all
  - name: Debian
    versions:
    - all

  galaxy_tags: [nfs,nfs-server,dev]

dependencies: []
